#include <stdio.h>
int main()
{
	float l,b,area;
	printf("Enter the value of length & breadth of a rectangle:");
	scanf("%f %f",&l,&b);
	area=l*b;
	printf("The area of rectangle is:%f",area);
	getch();
	
}
